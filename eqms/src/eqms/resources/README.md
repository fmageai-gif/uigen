# Resources

Optional bundled assets.

- `app.ico` — place a Windows icon here to brand the packaged executable. The
  PyInstaller spec picks it up automatically if present (and silently builds
  without one if absent).
- Additional branding images (logos, splash) can live here and be referenced
  from the UI/theme layer.

Keep these files small; they are bundled into the single-file `.exe`.
